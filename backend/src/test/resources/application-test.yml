spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/health-reservation-system
    username: postgres
    password: asd
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true

app:
  jwtSecret: TestSecretKeyForJWTwhichislongenough!
  jwtExpirationMs: 3600000

logging:
  level:
    org.springframework: DEBUG
    com.justynagajdek: DEBUG
